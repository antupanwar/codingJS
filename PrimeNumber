function IsPrime(n){
  let isPrime=true;
  for(i=2;i<n-1;i++){
    if(n%i==0){
      isPrime=false;
      break;
    }
  }
  if(isPrime){
    console.log(n, " is Prime number");
  }
  else{
    console.log(n, " is not Prime number");
  }
}
IsPrime(13);
